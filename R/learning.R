# Basics of R -------------------------------------------------------------

colnames(airquality)
str(airquality)
summary(airquality)

# Automatic styling
1 + 1 # highlight and ctrl+shift+a gives:
1 + 1

mean(2:6) # ctrl+shift+p --> style --> style_active_file (will alter the whole file, you're working in )

# Loading packages --------------------------------------------------------

library(tidyverse)

#This will be used for testing out Git


#Testing pull-push to GitHub
#Re-testing push pull after connecting to GitHub completed

# When a conflict happens

